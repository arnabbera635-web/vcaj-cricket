import {db,collection,onSnapshot} from './firebase.js';const list=document.getElementById('list'),search=document.getElementById('search');let data=[];const esc=s=>String(s??'').replace(/[&<>\"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;'}[c]));function load(){onSnapshot(collection(db,'matches'),s=>{data=s.docs.map(d=>({id:d.id,...d.data()})).sort((a,b)=>String(b.updatedAt?.seconds||b.updatedAt||'').localeCompare(String(a.updatedAt?.seconds||a.updatedAt||'')));render()},()=>{list.innerHTML='<div class=\"empty\">Live data unavailable.</div>'})}
search.oninput=render;load();
