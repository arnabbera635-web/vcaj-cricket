import {db,collection,getDocs} from './firebase.js';const box=document.getElementById('teams'),esc=s=>String(s??'').replace(/[&<>\"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;'}[c]));async function load(){const s=await getDocs(collection(db,'teams'));const a=s.docs.map(d=>({id:d.id,...d.data()})).sort((x,y)=>(x.name||'').localeCompare(y.name||''));box.innerHTML=a.length?a.map(t=>`<a href="teams.html?team=${encodeURIComponent(t.id)}" class="nav-tile"><strong>${esc(t.name)}</strong><span>${esc(t.shortName||'')} • ${t.playersCount||0} players</span></a>`).join(''):'<div class="empty">No teams yet. Admin portal থেকে দল যোগ করুন।</div>'}load();
